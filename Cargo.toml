[package]
name = "cuda-codec"
version = "0.1.0"
edition = "2021"
description = "Encoding and decoding — JSON, binary, varint, LEB128, chunked encoding, and protocol framing for agent communication"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
